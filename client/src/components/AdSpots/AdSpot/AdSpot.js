import React from 'react';
import useStyles from './AdSpotStyles.js';

const AdSpot = () => {
    const classes = useStyles();

    return (
        <h1>AdSpot</h1>
    );
};

export default AdSpot;